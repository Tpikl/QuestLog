import React from 'react';

import './Navbar.scss';

export const Navbar = () => {
    return (
        <nav>
            <a href='/'>Home</a>
            <a href='/weekly'>Weekly</a>
        </nav>
    );
}